import { Measurement } from "./types";

/**
 * Resolves the passed measurment into a single number representing that measurment's equivalent in pixels.
 * @param meaurment The meaurment to convert to pixels.
 */
export const resolveMeasurments = (meaurment: Measurement): number => {
    return 0;
}